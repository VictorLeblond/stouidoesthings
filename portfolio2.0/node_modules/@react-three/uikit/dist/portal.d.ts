import { ReactNode, RefAttributes } from 'react';
import type { EventHandlers } from '@react-three/fiber/dist/declarations/src/core/events.js';
import type { ImageProperties, PointerEventsProperties } from '@pmndrs/uikit/internals';
import type { ComponentInternals } from './ref.js';
export declare const privateKeys: string[];
type BasePortalProperties = Omit<ImageProperties, 'src' | 'objectFit'>;
export type PortalProperties = {
    frames?: number;
    renderPriority?: number;
    eventPriority?: number;
    /**
     * ratio between the size (in pixels) and the size of the render target (in pixels)
     * higher dpr means higher resolution of the render target
     */
    dpr?: number;
    children?: ReactNode;
} & BasePortalProperties & EventHandlers & {
    children?: ReactNode;
} & PointerEventsProperties;
export declare const Portal: (props: PortalProperties & RefAttributes<ComponentInternals<BasePortalProperties & EventHandlers>>) => ReactNode;
export {};
