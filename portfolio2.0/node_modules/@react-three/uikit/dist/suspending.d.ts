import { ReactNode, RefAttributes } from 'react';
import { ImageProperties } from './image.js';
import { ComponentInternals } from './ref.js';
export type SuspendingImageProperties = ImageProperties & {
    src: string;
    children?: ReactNode;
};
export declare const SuspendingImage: (props: SuspendingImageProperties & RefAttributes<ComponentInternals<Omit<ImageProperties, 'src'>>>) => ReactNode;
